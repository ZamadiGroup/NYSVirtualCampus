services:
  - type: web
    name: nys-virtual-campus
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npx tsx server/index.ts
    envVars:
      - key: NODE_ENV
        value: production
