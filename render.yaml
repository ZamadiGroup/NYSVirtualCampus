services:
  - type: web
    name: nys-virtual-campus
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: cd backend && npx tsx server/index.ts
    envVars:
      - key: NODE_ENV
        value: production
