# Database Configuration
# Option 1: MongoDB Atlas (Cloud)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nys-virtual-campus?retryWrites=true&w=majority

# Option 2: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/nys-virtual-campus

# Option 3: PostgreSQL with Drizzle ORM (if using Postgres instead of MongoDB)
# DATABASE_URL=postgresql://user:password@localhost:5432/nys_virtual_campus

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret for authentication
JWT_SECRET=your-secret-key-here
